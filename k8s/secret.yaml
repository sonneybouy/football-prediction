apiVersion: v1
kind: Secret
metadata:
  name: football-prediction-secrets
  namespace: football-prediction
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  # echo -n "your_api_key" | base64
  FOOTBALL_API_KEY: ""
  
  # Database URL - replace with actual values
  # echo -n "postgresql://user:pass@host:5432/db" | base64
  DATABASE_URL: ""
  
  # Redis URL - replace with actual values  
  # echo -n "redis://redis:6379/0" | base64
  REDIS_URL: ""